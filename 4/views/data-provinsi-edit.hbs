<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <title>Edit Data Provinsi</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Home</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            {{#if user}}
                <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Tambah Data
                </a>
                <ul class="dropdown-menu">
                  <li><a class="dropdown-item" href="/data-provinsi-add">Data Provinsi</a></li>
                  <li><a class="dropdown-item" href="/data-kabupaten-add">Data Kabupaten</a></li>
                </ul>
              </li>
            {{/if}}    
              
             
            </ul>
            {{#if user}}
            <div class="d-flex" >
              <h3 style="margin-right:15px">{{user.user_name}}</h3>
              <form action="/logout" method="post"><button class="btn btn-outline-danger" type="submit">Logout</button></form>
            </div>
            {{else}}
            <div class="d-flex" style="margin-right:15px">
              <a href="/login"><button class="btn" type="submit">Login</button></a>
            </div>
            <div class="d-flex" style="margin-right:15px">
              <a href="/register"><button class="btn" type="submit">Register</button></a>
            </div>
            {{/if}}
          </div>
        </div>
      </nav>

    <div class="container d-flex gap-2 mt-5 flex-column" style="width: 45%;">
      <form action="/data-provinsi-edit/{{data_query.id}}" method="post" enctype="multipart/form-data">
        <label for="nama" class="form-label fw-bold">Nama</label>
        <input type="text" name="nama" id="nama" class="form-control" value="{{data_query.nama}}" required> 
        <label for="nama" class="form-label fw-bold">Diresmikan</label>
        <input type="date" name="diresmikan" id="nama" class="form-control" required>
        <label for="nama" class="form-label fw-bold">Pulau</label>
        <input type="text" name="pulau" id="nama" class="form-control" value="{{data_query.pulau}}" required>
        <div class="d-flex flex-column gap-2 mt-3">
          <label class="form-label fw-bold">Photo</label>
        <input type="file" name="photo" id="photo" required>
        <button type="submit" class="btn btn-dark" style="width: 100px; margin-top: 25px;">Submit</button>
        </div>
        
      </form>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
